// unfixable error: unused variable
const unusedVariable = "this will cause an error";

// unfixable error: implicit any type
function badFunction(param) {
  return param;
}

export default badFunction;
